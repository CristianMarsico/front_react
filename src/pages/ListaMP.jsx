import GeneralTablaMP_Components from '../components/Tablas/TablaMateriaPrima/GeneralTablaMP_Components'

/**
 * Componente que representa la página de lista de materia prima.
 * @returns {JSX.Element} Elemento JSX que representa la página de materia prima.
 */
const ListaMP = () => {
    return (
        <GeneralTablaMP_Components />
    )
}

export default ListaMP