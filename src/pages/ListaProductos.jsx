import React from 'react'
import GeneralHilado_Components from '../components/Tablas/TablaHilado/GeneralHilado_Components'

/**
 * Componente que representa la página de lista de productos terminados.
 * @returns {JSX.Element} Elemento JSX que representa la página de productos terminados.
 */
const ListaProductos = () => {
    return (
        <GeneralHilado_Components />
    )
}

export default ListaProductos